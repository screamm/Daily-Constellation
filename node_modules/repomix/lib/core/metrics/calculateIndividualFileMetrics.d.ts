import type { RepomixProgressCallback } from '../../shared/types.js';
import type { ProcessedFile } from '../file/fileTypes.js';
import type { TokenCounter } from '../tokenCount/tokenCount.js';
export interface FileMetrics {
    path: string;
    charCount: number;
    tokenCount: number;
}
export declare const calculateIndividualFileMetrics: (file: ProcessedFile, index: number, totalFiles: number, tokenCounter: TokenCounter, progressCallback: RepomixProgressCallback) => Promise<FileMetrics>;
//# sourceMappingURL=calculateIndividualFileMetrics.d.ts.map