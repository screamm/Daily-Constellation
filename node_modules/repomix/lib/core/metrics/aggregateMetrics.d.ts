import type { ProcessedFile } from '../file/fileTypes.js';
import type { TokenCounter } from '../tokenCount/tokenCount.js';
import type { FileMetrics } from './calculateIndividualFileMetrics.js';
export declare const aggregateMetrics: (fileMetrics: FileMetrics[], processedFiles: ProcessedFile[], output: string, tokenCounter: TokenCounter) => {
    totalFiles: number;
    totalCharacters: number;
    totalTokens: number;
    fileCharCounts: Record<string, number>;
    fileTokenCounts: Record<string, number>;
};
//# sourceMappingURL=aggregateMetrics.d.ts.map